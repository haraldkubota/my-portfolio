{
  "name": "upload",
  "version": "1.0.0",
  "description": "Upload files from the build zip into S3 web bucket",
  "main": "upload.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Harald Kubota",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.112.0",
    "stream": "0.0.2",
    "yauzl": "^2.8.0"
  }
}
